{
  "name": "basic-react-typescript-app",
  "version": "1.0.0",
  "description": "Basic HTML + React + TypeScript app",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --minify --outfile=dist/bundle.js",
    "build:watch": "esbuild src/index.tsx --bundle --outfile=dist/bundle.js --watch"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tweetnacl": "^1.0.3",
    "session-keystore": "^1.0.3"
  }
}